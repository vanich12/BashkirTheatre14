<UserControl x:Class="BashkirTheatre14.View.Pages.QuizPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BashkirTheatre14.View.Pages"
      xmlns:controls="clr-namespace:BashkirTheatre14.ViewModel.Controls"
      xmlns:pages="clr-namespace:BashkirTheatre14.ViewModel.Pages"
      xmlns:helpers="clr-namespace:BashkirTheatre14.Helpers"
      xmlns:converter="clr-namespace:BashkirTheatre14.Converter"
      mc:Ignorable="d" 
      d:DesignHeight="3840" d:DesignWidth="2160"
      d:DataContext="{d:DesignInstance controls:QuizViewModel}"
     >
    <UserControl.Resources>
        <helpers:QuizTemplateSelector x:Key="QuizTemplateSelector" />
    </UserControl.Resources>
 
    <Grid>
        <ContentControl 
            Content="{Binding }"
            ContentTemplateSelector="{StaticResource QuizTemplateSelector}">
        </ContentControl>
    </Grid>
</UserControl>
